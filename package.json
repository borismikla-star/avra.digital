{
  "name": "avra-digital",
  "version": "1.0.0",
  "description": "AVRA — Agent for AR/VR in Realty",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "setup": "pip3 install pdfplumber Pillow numpy --break-system-packages || pip install pdfplumber Pillow numpy"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^2.0.0",
    "lowdb": "^1.0.0",
    "qrcode": "^1.5.3",
    "cors": "^2.8.5",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
