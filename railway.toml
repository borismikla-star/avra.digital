[build]
builder = "nixpacks"
buildCommand = "npm install && pip3 install pdfplumber Pillow numpy pdf2image || true"

[deploy]
startCommand = "node server/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "avra-digital"
